<div class="container">
  <h1>List of categories</h1>

  <table class="table table-striped">
    <tr>
      <th>Id</th>
      <th>Name</th>
    </tr>
    <% @categories.each do |c| %>
      <tr>
        <td><%= c.id %></td>
        <td><%= c.name %></td>
      </tr>
    <% end %>
  </table>
</div>
